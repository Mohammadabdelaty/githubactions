name: Scheduled Jobs
on:
  schedule:
    - cron: '*/1 * * * *'  # Runs every 1 minutes

jobs:
  scheduled-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5.0.0

      - name: Run a script
        run: |
          echo "This job runs every 1 minutes"
          # Add your script or commands here